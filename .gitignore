/2059.pdf
/docs
/fx mark folder.docx
/FXMARK_FULL_SYSTEM_CODE.zip
/infrastructure

# Docker backups (image tarballs)
docker/backups/

# Dependencies and build
node_modules/
**/node_modules/
dist/
**/dist/
build/
.env
.env.local
# Local / staging overrides (templates stay as `.env.example` only)
.env.staging
**/.env.staging
**/.env.development

# Mongo dumps & DB exports (large / may contain PII)
backend/dump/
backend/dump_test_only/

# Application runtime logs
backend/logs/

# Backend local scratch probes (never deploy)
backend/_*.mjs
backend/_*.json
*.log
# Local export (may contain emails / account IDs)
backend/user-account-ids.txt
backend/user-records-full.txt
.DS_Store