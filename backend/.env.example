# Twelve Data API key (required)
TWELVE_DATA_API_KEY=your_api_key_here

# Redis connection (optional - uses in-memory cache when not set)
# REDIS_URL=redis://localhost:6379

# Server port (default: 3000)
PORT=3000
