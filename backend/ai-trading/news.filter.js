/**
 * News filter
 * Filter trades around high-impact news
 */
function shouldPause(symbol, newsEvents) {
  // TODO: check upcoming news for symbol
  return false;
}

module.exports = { shouldPause };
