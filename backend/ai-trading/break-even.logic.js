/**
 * Break-even logic
 * Move SL to entry when in profit by X
 */
function shouldMoveToBreakeven(entry, currentPrice, threshold) {
  // TODO: compare with threshold
  return false;
}

module.exports = { shouldMoveToBreakeven };
