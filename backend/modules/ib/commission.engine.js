/**
 * IB commission engine
 * Calculate commission per trade by level/volume
 */
async function calculate(trade, ibId) {
  // TODO: level rate, volume, currency
  return { amount: 0, currency: 'USD' };
}

module.exports = { calculate };
