# FXMARK Frontend (build + serve)
FROM node:20-alpine AS builder
WORKDIR /app
COPY frontend-web/package*.json ./
RUN npm ci
COPY frontend-web/ .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
